<Window x:Class="ChatWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="300"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Margin="4" Grid.Row="1" Grid.Column="1">
            <Label Content="Введите логин"/>
            <TextBox x:Name="userTextBox" />
            <Label Content="Введите сообщение"/>
            <TextBox x:Name="messageTextBox" />
            <Button x:Name="sendBtn" Content="Отправить" Margin="10" Width="300" Click="Button_Click" IsEnabled="False" />
            <ListBox x:Name="chatbox" Height="165"/>
        </StackPanel>
    </Grid>
</Window>
